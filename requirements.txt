# New Streamlined Chatbot Requirements
# Phase 2: bumped for Python 3.12+ (use after Option B Phase 1)

# Web Framework
Flask==3.1.3
Werkzeug==3.1.8

# Database — REQUIRED for Postgres (``services/database_service.py`` imports ``psycopg2``).
# Install ``psycopg2-binary`` (prebuilt wheels); do not remove. On PythonAnywhere run:
#   pip install -r requirements.txt
# Note: PyPI package ``psycopg`` is Psycopg v3 (different API); this project uses v2 via ``psycopg2-binary``.
psycopg2-binary>=2.9.11,<3

# AI Services
anthropic==0.92.0
pydantic>=2.0.0,<3  # used directly in infrastructure/security/payload_validator.py
# Legacy ``google-generativeai`` is deprecated; unified Gemini SDK
google-genai>=1.20,<2

# Google Services
google-auth==2.49.1
google-auth-oauthlib==1.3.1
google-auth-httplib2==0.3.1
google-api-python-client==2.194.0
google-cloud-vision==3.13.0
googlemaps==4.10.0  # For outcall address verification and travel time calculation

# Environment and Config
python-dotenv==1.2.2

# Safe XML parsing (admin Config uploads / ElementTree hardening)
defusedxml>=0.7.1,<1

# Date/Time
python-dateutil==2.9.0.post0
pytz==2026.1.post1

# Observability (optional: set SENTRY_DSN)
sentry-sdk[flask]>=2.0.0,<3

# Utilities (2.32+ avoids urllib3/charset_normalizer version warnings)
requests>=2.31.0,<3
tenacity>=8.2.0,<10
beautifulsoup4>=4.12.3,<5
cloudscraper>=1.2.71,<2

# Image upload validation (deposit screenshots)
# Pillow upper-bound capped aggressively: frequent CVE source (CVE-2023-50447 etc.)
Pillow>=10.0.0,<12
openpyxl>=3.1.5,<4

# Background Jobs
APScheduler==3.11.2
# Cap major psutil jumps until explicitly tested (7.x OK; avoid surprise 8+)
psutil>=5.9.0,<8

# Optional: hosted Redis for distributed rate limiting (Upstash, Redis Cloud, etc.)
# Use rediss:// for TLS. PythonAnywhere can connect to external Redis; no local Redis required.
redis>=5.0.0,<6

# SMS gateway encryption (Fernet AES-128-CBC + HMAC-SHA256)
cryptography>=44.0.0,<47

# Password hashing (argon2id is the PHC-winner, current best practice)
argon2-cffi>=23.1.0,<24

# Admin 2FA — TOTP codes + QR code for authenticator app enrollment
pyotp>=2.9.0,<3
qrcode[pil]>=7.4.2,<9

# Optional: pgvector Python adapter for cosine similarity search.
# Only required when EMBEDDINGS_ENABLED=true AND the PostgreSQL server has the
# pgvector extension installed (CREATE EXTENSION vector).
# Install: pip install pgvector
# pgvector>=0.3.0,<1
